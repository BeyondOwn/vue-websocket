{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "routes": [
    { "src": "/login", "dest": "/" },
    { "src": "/login/(.*)", "dest": "/" }
  ],
  "builds": [{ "src": "package.json", "use": "@vercel/static-build" }]
}
